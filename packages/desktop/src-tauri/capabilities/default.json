{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default permissions for YAA Desktop",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default",
    "dialog:allow-open"
  ]
}
